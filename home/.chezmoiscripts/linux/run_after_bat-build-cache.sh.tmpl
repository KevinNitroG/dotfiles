#!/usr/bin/env bash

# config hash: {{ include ".config/bat/config" | sha256sum }}
# themeFile hash: {{ include (printf ".config/bat/%s" .catppuccinFlavor) | sha256sum }}

if ! command -v bat &>/dev/null; then
  exit
fi

echo "BUILDING BAT CACHE..."
bat cache --build
